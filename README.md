# Cosmos Snapshots

    git clone git@github.com:NodeSphereGL/cosmos-snapshots.git
    cd cosmos-snapshots/scripts
    chmod +x *.sh

# Create new snapshot

    cd cosmos-snapshots/scripts
    ./shentu_snapshot.sh
